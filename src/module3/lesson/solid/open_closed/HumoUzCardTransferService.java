package module3.lesson.solid.open_closed;

public class HumoUzCardTransferService implements TransferService {
    @Override
    public boolean transfer(Transfer transfer) {
        /// ..
        return false;
    }
}
